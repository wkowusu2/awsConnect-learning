version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci

  build:
    commands:
      - npm run build
      - npm test --if-present

artifacts:
  files:
    - dist/**/*
    - package.json
    - package-lock.json